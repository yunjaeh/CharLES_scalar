# make sure your png images have even width and height: nx x ny

ffmpeg -f image2 -pattern_type glob -i '*.png' -vcodec libx264 -pix_fmt yuv420p -crf 1  output.mp4

# you can also generate at half-size with the following example. In this case,
# your original image dimensions will need to be divisible by 4...

ffmpeg -f image2 -pattern_type glob -i 'magu_vm_final/tile.000*png' -vf "scale=iw/2:ih/2" -vcodec libx264 -pix_fmt yuv420p -crf 1 magu_vm_final_half.mp4
